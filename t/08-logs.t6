use Test;
use Test::When <author>;
use Docker::API;
use LibYAML:auth<github:CurtTilmes>;

my $docker = Docker::API.new();

print $docker.container-logs(id => 'bde083ce1d70', :stdout).stdout.decode;


done-testing;
