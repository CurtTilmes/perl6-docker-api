use Test;
use Test::When <author>;
use Docker::API;
use JSON::Fast;
my $docker = Docker::API.new();

#plan 5;

say to-json($docker.auth(username => 'ctilmes',
                 password => %*ENV<DOCKER_PW>,
                 email => 'curt@tilmes.org',
                         serveraddress => "https://index.docker.io/v1/"));

done-testing;
